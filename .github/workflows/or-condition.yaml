name: Or Condition Demo
on: push
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Run for hithesh-27 or Javahometech
        if: github.actor == 'hithesh-27' || github.actor == 'javahometech'
        run: echo "It is triggered by either hithesh-27 or Javahometech"
